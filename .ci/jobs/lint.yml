lint-frontend:
  stage: lint
  cache:
    key: shared-pnpm
    paths:
      - .pnpm-store
      - node_modules
  script:
    - pnpm lint
  rules:
    - if: '$CI_COMMIT_BRANCH != "dev"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
