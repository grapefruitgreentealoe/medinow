deploy-frontend-dev:
  stage: deploy
  image: node:20
  before_script:
    - apt-get update && apt-get install -y docker.io
  script:
    - set -xe
    - echo "📦 배포 스크립트 실행 시작"
    - mkdir -p ./scripts
    - chmod +x ./scripts/deploy-frontend-dev.sh
    - ./scripts/deploy-frontend-dev.sh
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev" && $CI_PIPELINE_SOURCE == "push"'

deploy-frontend-prod:
  stage: deploy
  image: node:20
  before_script:
    - apt-get update && apt-get install -y docker.io
  script:
    - set -xe
    - echo "📦 배포 스크립트 실행 시작"
    - mkdir -p ./scripts
    - chmod +x ./scripts/deploy-frontend-prod.sh
    - ./scripts/deploy-frontend-prod.sh
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
